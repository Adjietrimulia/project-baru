<!DOCTYPE html>
<html>
<head>
    <title>Sistem Perhitungan Ongkir</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #eef2f3;
            padding: 30px;
        }
        h2 {
            text-align: center;
            color: #333;
        }
        form {
            background: white;
            padding: 20px;
            border-radius: 10px;
            max-width: 400px;
            margin: 0 auto;
            box-shadow: 0px 3px 8px rgba(0,0,0,0.2);
        }
        label {
            display: block;
            margin-top: 10px;
            font-weight: bold;
        }
        select, input[type="number"] {
            width: 100%;
            padding: 8px;
            border-radius: 6px;
            border: 1px solid #ccc;
            margin-top: 5px;
        }
        input[type="submit"] {
            margin-top: 15px;
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px;
            width: 100%;
            border-radius: 6px;
            cursor: pointer;
        }
        .hasil {
            margin-top: 20px;
            background: #fff;
            border-radius: 10px;
            padding: 15px;
            box-shadow: 0px 3px 8px rgba(0,0,0,0.2);
        }
    </style>
</head>
<body>
    <h2>💸 Sistem Perhitungan Ongkir</h2>
    <form method="POST">
        <label>Berat Barang (kg):</label>
        <input type="number" step="0.1" name="berat" required>

        <label>Zona Pengiriman:</label>
        <select name="zona" required>
            <option value="">-- Pilih Zona --</option>
            <option value="jabodetabek">Jabodetabek</option>
            <option value="nonjabodetabek">Non-Jabodetabek</option>
        </select>

        <label>Layanan Pengiriman:</label>
        <select name="layanan" required>
            <option value="reguler">Reguler</option>
            <option value="sameday">Same Day Delivery</option>
        </select>

        <input type="submit" name="hitung" value="Hitung Ongkir">
    </form>

    <?php
    if (isset($_POST['hitung'])) {
        $berat = $_POST['berat'];
        $zona = $_POST['zona'];
        $layanan = $_POST['layanan'];

        if ($berat <= 0) {
            echo "<div class='hasil'><b>⚠ Error:</b> Berat barang tidak valid!</div>";
        } else {
            // Tarif dasar
            if ($zona == "jabodetabek") {
                $tarifDasar = 8000;
            } elseif ($zona == "nonjabodetabek") {
                $tarifDasar = 12000;
            } else {
                $tarifDasar = 0;
            }

            // Hitung ongkir
            if ($berat <= 1) {
                $total = $tarifDasar;
            } else {
                $tambahan = ceil($berat - 1) * 5000;
                $total = $tarifDasar + $tambahan;
            }

            // Tambahan biaya sameday
            if ($layanan == "sameday") {
                $total += 10000;
            }

            echo "<div class='hasil'>
                    <h3>💰 Hasil Perhitungan Ongkir:</h3>
                    <p>Zona Pengiriman: <b>" . ucfirst($zona) . "</b></p>
                    <p>Layanan: <b>" . ucfirst($layanan) . "</b></p>
                    <p>Berat Barang: <b>$berat kg</b></p>
                    <p>Total Ongkir: <b>Rp " . number_format($total, 0, ',', '.') . "</b></p>
                  </div>";
        }
    }
    ?>
</body>
</html>
